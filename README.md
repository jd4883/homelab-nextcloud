# homelab-nextcloud
Nextcloud deployment for the homelab: Helm chart, optional OIDC/ingress and storage config, and docs for file sync, Calendar, Contacts, and optional apps. Designed to run on Kubernetes (e.g. Talos) with your existing auth and storage.
